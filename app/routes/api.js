module.exports = app => {
    const auctions = require("../controllers/user.controller.js");
  
    var router = require("express").Router();
  
    // Create a new 
    router.post("/", );
  
    // Retrieve all 
    router.get("/", );
  
    app.use('/api', router);
};
